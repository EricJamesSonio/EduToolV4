The semester setting is not in the department management itself. instead move it in the department level
Since some departments such as techvoc and BSCS
The bscs has semester of starting in june and end in march
while techvoc has different like start is august, end is MAy.
so yeah so each department has its own settings of semester.


And also.
Since we need to validate and check that in schedule,. the educator shouldn't be having conflic like in schedule for example., the educator has same day class in two classes.like 7am data structure for 1st yr monday, thgen 7am for parallel in 3rd yr monday alsop. so the checker is active checking also the other years. so it's checking all yr schdule of the department itself. since we cannot do that if the checker only scoped inside each yr. so we have checker for all yrs sicn e some prof handles multiple yr in a department