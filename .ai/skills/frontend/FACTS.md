# Frontend Skill — Facts

<!--
Project-specific facts only. Permanent data-fetching/SSR/state/component
rules live in shared/skills/frontend/MUST-HAVES.md — read that first.
shared/agents/onboarder.md is the only agent that writes to this file.
-->

Last verified: <!-- fill in --> UNKNOWN — needs onboarder run

## Framework

Framework: <!-- e.g. Next.js App Router -->
Rendering: <!-- e.g. Server Components by default, Client Components opt-in via "use client" -->

## Data fetching pattern

<!-- e.g. Server-side fetching preferred; client fetching only for
interactive/real-time data. Name the actual shared API client file, e.g.
hooks/useAppQuery.ts. -->

## State management

<!-- e.g. Local state via useState/useReducer; shared state via
Context/Zustand/Redux — name what's actually imported, don't assume. -->

## Component conventions

<!-- Naming conventions, folder structure, styling approach — fill in per
project. -->
