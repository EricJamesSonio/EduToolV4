# Testing Skill — Facts

<!--
Project-specific facts only. Every test-writing convention, test-selection
rule, and scope level lives in shared/skills/testing/MUST-HAVES.md — read
that first. shared/agents/onboarder.md is the only agent that writes to
this file. Confirmed by config file, not assumed.
-->

Last verified: <!-- fill in --> UNKNOWN — needs onboarder run

## Stack

Unit/integration: <!-- e.g. Jest — confirm via jest.config.*/vitest.config.* -->
E2E: <!-- e.g. Playwright/Cypress — confirm via playwright.config.*/cypress.config.*, or "none" if no e2e config exists -->
