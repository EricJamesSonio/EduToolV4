Here's the full plan, step by step.

Backend Steps
Step 1 — DTOs (subject.dto.ts)

Add subjectType?: 'major' | 'minor' and programId?: string to CreateSubjectDto
Add subjectType?: string to QuerySubjectDto
New ShareSubjectDto — { courseId?, strandId?, levelId? } (exactly one required)

Step 2 — Repository (subject.repository.ts)

create — save subject_type and program_id
findAll — filter by subjectType; for minor subjects include sharings in the response
findById — include sharings with course/strand/level names
New addSharing(subjectId, orgId, target) — insert into SubjectSharing
New removeSharing(sharingId, orgId) — delete from SubjectSharing
New findSharings(subjectId, orgId) — list all sharings for a minor subject

Step 3 — Service (subject.service.ts)

create — pass subjectType, programId through
findAll — pass subjectType filter through
mapToResponse — include subjectType, programId, sharings in response shape
New share(id, orgId, dto) — validate subject is minor, validate target is in same program, call repo
New unshare(id, sharingId, orgId) — validate ownership, call repo
New findSharings(id, orgId) — call repo

Step 4 — Controller (subject.controller.ts)

Fix the commented-out @Get(), @Get(':id'), @Patch(':id') routes (they were accidentally commented with /**)
New POST /subjects/:id/share
New DELETE /subjects/:id/share/:sharingId
New GET /subjects/:id/sharings


Frontend Steps
Step 5 — Types (types/admin/subject.types.ts)

Add subjectType: 'major' | 'minor'
Add programId: string | null
Add sharings: SubjectSharing[]
New SubjectSharing interface — { id, courseId, courseName, strandId, strandName, levelId, levelName }

Step 6 — API (api/admin/subject.api.ts)

CreateSubjectRequest — add subjectType, programId
GetSubjectsQuery — add subjectType
getAll mapper — map new fields
New share(id, { courseId?, strandId?, levelId? })
New unshare(id, sharingId)
New getSharings(id)

Step 7 — Subjects list page (app/admin/subjects/page.tsx)

Add two tabs: Major and Minor — tab switch sets subjectType filter passed to API
Minor tab shows a "Shared to" column with badges (course/strand/level names)
"New Subject" button on Minor tab opens dialog pre-set to subjectType: minor

Step 8 — Subject dialog (components/admin/subject/SubjectDialog.tsx)

Add subjectType toggle (Major / Minor)
When Minor is selected, show programId selector (dropdown of programs for the selected school year)
Pass subjectType and programId to the create API call

Step 9 — Subject detail page (app/admin/subjects/[id]/page.tsx)

Show subjectType badge (Major / Minor) in the header
For minor subjects: show a Shared To section listing all sharings with remove buttons
Add a Share button that opens ShareSubjectDialog

Step 10 — New: ShareSubjectDialog (components/admin/subject/ShareSubjectDialog.tsx)

Fetches courses/strands/levels scoped to the minor subject's program
Shows checkboxes for each — pre-checks already-shared ones (disabled)
On confirm calls subjectApi.share(id, target) for each newly checked item


Order to implement
1. subject.dto.ts
2. subject.repository.ts
3. subject.service.ts
4. subject.controller.ts
5. subject.types.ts
6. subject.api.ts
7. subjects/page.tsx  (tabs)
8. SubjectDialog.tsx  (type + programId)
9. subjects/[id]/page.tsx  (sharings section)
10. ShareSubjectDialog.tsx  (new component)

Ready to start? Just say "go" and I'll write Steps 1–4 (all backend) in one shot, then we move to frontend.