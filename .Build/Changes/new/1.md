New modules to create: course, strand, and subject-prerequisite — these are entirely new DB models with no existing module.
Existing modules to update:

subject — new fields (course_id, strand_id, year_level, term_label) + new prerequisite relations → DTOs, entity, repository, service all need touching
program — now has courses[] and strands[] relations → entity and repository need updating
enrollment — needs prerequisite validation logic in the service before allowing enrollment

  ::view-transition-group(*),
  ::view-transition-old(*),
  ::view-transition-new(*) {
    animation-duration: 0.25s;
    animation-timing-function: cubic-bezier(0.19, 1, 0.22, 1);
  }
VvisualizeVvisualize show_widgetHere's the full rundown of what to do:
Create from scratch — three new modules following your existing pattern (dto / entity / repo / service / controller / module):

src/modules/course/ — CRUD for courses under a program
src/modules/strand/ — same structure, for SHS strands
src/modules/subject-prerequisite/ — manages the prereq join table, with a service method that validates whether a student meets prereqs before enrollment

Update existing modules:

subject/ — entity gets the 4 new fields + both relation sides. DTOs need course_id?, strand_id?, year_level?, term_label?. Repository needs a scoped query like findByScope(org_id, course_id?) that returns open subjects + that course's majors.
program/ — entity adds courses Course[] and strands Strand[]. Repository's findOne should include those relations so admin can see what's under each program.
class/ enrollment service — before inserting an Enrollment, call the prerequisite service to check all SubjectPrerequisite rows for that subject, then verify each prerequisite_id has a locked passing Grade for that student. Block and return the missing ones if not.

Domain modules:

academic-domain.module.ts — import and re-export CourseModule, StrandModule, SubjectPrerequisiteModule
class-domain.module.ts — import SubjectPrerequisiteModule so enrollment can use its service

New seed file — src/database/seeds/program.seed.ts implementing the two-pass seeder (subjects first, then prereqs by resolved ID) following the CourseSeed / ProgramSeed types from the plan doc.
app.module.ts and main.ts don't need structural changes — just make sure the new modules are wired into the right domain module.