All 6 files refactored from your existing code. Here's what changed in each:
repository.ts — full rewrite for new schema. All school_year_id lookups on settings are gone. Added findAllSettings, findLocksBySchoolYear, findExpiredUnlockedLocks, findUnlockedLocksWithSchoolYear, createEvent, findEventsByClassId. DatabaseService is the only dependency.
service.ts — no more DatabaseService injection, calls repo only. Added createSetting, updateSetting, deleteSetting, assignSetting, autoAssignOnClassCreate, getEventsForClass, overrideLock. autoLockExpiredClasses now handles both lock_deadline and deadlineDays cases. mapClassToGradeLock renamed to mapLock, deadline is now populated from setting. Audit log calls replaced with repo.createEvent.
validator.ts — now injects GradeLockRepository instead of DatabaseService. Lookup goes through GradeLock → setting directly, no more school_year_id join.
controller.ts — added PUT settings/:id, DELETE settings/:id, POST assign, POST :classId/override, GET :classId/events. Same import paths as your original.
module.ts — AuditLogModule removed since events are now GradeLockEvent. GradeLockValidator added to exports so other modules can use it.
One thing you need to add — in class.service.ts, call gradeLockService.autoAssignOnClassCreate(orgId, createdClass.id, dto.setting_id) after class creation, and add optional setting_id?: string to your CreateClassDto.Grade lock.serviceTS DownloadGrade lock.repositoryTS DownloadGrade lock.controllerTS DownloadGrade lock.dtoTS DownloadGrade lock.validatorTS DownloadGrade lock.moduleTS DownloadDownload all