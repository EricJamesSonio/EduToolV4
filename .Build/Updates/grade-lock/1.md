🔴 **STRICT ENGINEER MODE - PLANNING PHASE**

I'll create a comprehensive **migration plan** before ANY code changes.

---

## 📋 **COMPLETE SCOPE ANALYSIS**

### **Database Schema Changes**

```
CURRENT:
GradeLockSetting {
  org_id
  school_year_id  ← REMOVE
  lock_deadline
  @@unique([org_id, school_year_id])
}

NEW:
GradeLockSetting {
  org_id
  name           ← ADD (reusable template name)
  lockType       ← ADD (hard/soft/flexible)
  lock_deadline  ← KEEP or CHANGE to deadlineDays?
  allowOverride  ← ADD
  @@unique([org_id, name])  ← NEW constraint
}

CURRENT:
GradeLock {
  class_id @unique
  is_locked
  locked_by
  locked_at
}

NEW:
GradeLock {
  class_id @unique
  setting_id     ← ADD (FK to GradeLockSetting)
  is_locked
  locked_by
  locked_at
}
```

---

### **Backend Impact Matrix**

| Layer | File | Change Type | Impact |
|-------|------|-------------|--------|
| **Service** | `grade-lock.service.ts` | Major Refactor | 8 methods affected |
| **DTO** | `grade-lock.dto.ts` | Extend | Add `CreateGradeLockSettingDto` changes |
| **Controller** | `grade-lock.controller.ts` | Extend | New endpoints for template CRUD |
| **Validator** | `grade-lock.validator.ts` | Update | Validate new fields |
| **Module** | `grade-lock.module.ts` | No change | (likely) |

---

### **Frontend Impact Matrix**

**Pages (3):**
- [ ] `admin/grade-lock/page.tsx` — Update layout/flow
- [ ] `admin/grade-lock/settings/page.tsx` — NEW (manage templates)
- [ ] `admin/grade-lock/assignments/page.tsx` — NEW (assign to classes)

**Components (6+):**
- [ ] `GradeLockHierarchyFilter.tsx` — Refactor
- [ ] `GradeLockSettingModal.tsx` — Refactor (becomes template manager)
- [ ] `GradeLockOverrideDialog.tsx` — Update (reference setting)
- [ ] `GradeLockStats.tsx` — Update (new metrics)
- [ ] `GradeLockTemplateList.tsx` — NEW
- [ ] `GradeLockAssignmentModal.tsx` — NEW

**Hooks (3):**
- [ ] `useGradeLocks()` — Update query
- [ ] `useGradeLockSetting()` → `useGradeLockSettings()` — Update (plural, returns all)
- [ ] `useGradeLockAssignment()` — NEW (assign template to class)

**Types (2):**
- [ ] `grade-lock.types.ts` — Refactor
- [ ] Add `GradeLockSetting`, `GradeLockTemplate` interfaces

**API (4):**
- [ ] `grade-lock.api.ts` — New/updated endpoints
  - `GET /api/grade-lock/settings` ← Get all templates
  - `POST /api/grade-lock/settings` ← Create template
  - `PUT /api/grade-lock/settings/:id` ← Update template
  - `DELETE /api/grade-lock/settings/:id` ← Delete template
  - `POST /api/grade-lock/assign` ← Assign template to class

---

## 🏗️ **MIGRATION STEPS (Recommended Order)**

### **Phase 1: Database (Prisma)**
1. Update `GradeLockSetting` model
2. Update `GradeLock` model (add `setting_id`)
3. Create migration
4. Generate Prisma client

### **Phase 2: Backend Services**
1. Update `grade-lock.service.ts` methods:
   - `createSetting()` → `createTemplate()`
   - `getSetting()` → `getSettings()` (plural)
   - `getClassLocks()` — Update to join with setting
   - `lockClass()` — Update to use setting
   - Add `assignSettingToClass()`
   
2. Update `grade-lock.dto.ts`
3. Update `grade-lock.controller.ts` endpoints
4. Update `grade-lock.validator.ts`

### **Phase 3: Frontend Types & API**
1. Update `grade-lock.types.ts`
2. Update `grade-lock.api.ts` with new endpoints

### **Phase 4: Frontend Hooks**
1. Update existing hooks
2. Create new hooks

### **Phase 5: Frontend UI (Pages & Components)**
1. Refactor grade lock dashboard
2. Create settings/templates management page
3. Create class assignment interface

---

## ❓ **CRITICAL QUESTIONS BEFORE PROCEEDING**

**Q1: Date Logic**
```
Current: lock_deadline (DateTime)
Option A: Keep lock_deadline per template
Option B: Change to deadlineDays (Int) + calculate at runtime
Option C: Both (deadline + deadlineDays for flexibility)

Which approach?
```

**Q2: Backward Compatibility**
```
Do you need to:
A) Migrate existing GradeLockSettings?
B) Start fresh (delete old data)?

Answer affects migration SQL complexity.
```

**Q3: School Year Scope**
```
You said: "Multiple policies in ONE school year"

But do you ALSO want to filter policies by school year?
Example: "Show me settings applicable to SY 2025-2026"

Or: Policies are org-wide, class chooses which one?
```

**Q4: Assignment Timing**
```
When does a class get assigned a policy?
A) When class is created (add field to createClass())
B) Later, via admin panel (separate assignment page)
C) Both (optional, can update anytime)
```

**Q5: Lock Type Options**
```
You mentioned: "hard", "soft", "flexible"

Define the behavior:
- hard: Cannot override, deadline is final
- soft: Can request override, needs approval
- flexible: Can override, just needs notification

Correct?
```

---

## 📊 **Estimated Effort**

| Phase | Files | Complexity | Est. Time |
|-------|-------|------------|-----------|
| DB | 1 | Medium | 30 min |
| Backend | 5 | High | 2-3 hrs |
| Frontend Types/API | 2 | Medium | 45 min |
| Frontend Hooks | 3 | Medium | 1 hr |
| Frontend UI | 10 | High | 3-4 hrs |
| **Total** | **21** | **High** | **7-8 hrs** |

---

## ✅ **CONFIRMATION NEEDED**

Before I start implementation:

1. **Confirm the 5 questions above** ☝️
2. **Confirm you want me to do this in phases** (Phase by phase)
3. **Confirm priority** — Start with database + backend, or frontend first?

Once confirmed, I'll give you **exact surgical code blocks** with no hallucination. 🎯